<div class="container">
  <br>
  <h1 class="text-danger">Doctors: </h1>
  <% @doctors.each do |doctor| %>
    <h2> <%= link_to "#{doctor.name} - #{doctor.patient_count}", doctor_path(doctor), :class => "text-info"%> </h2>
  <% end %>
</div>
